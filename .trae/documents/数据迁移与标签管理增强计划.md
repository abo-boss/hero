### 🛠️ 数据修复与功能增强计划

目前数据没有显示是因为旧数据（如 `category: 'cc-video'`）与新系统（`category: 'content-creation', resourceType: 'video'`）不匹配。我将执行以下操作：

#### 1. 数据迁移 (Data Migration)
编写并运行脚本 `scripts/fix-data.ts`，将所有旧数据自动归位：

*   **AI 资源**:
    *   原 `category: 'podcast'` -> 改为 `category: 'ai'`, `resourceType: 'podcast'`
    *   原 `category: 'product'` -> 改为 `category: 'ai'`, `resourceType: 'tool'`
    *   原 `category: 'ai'` (未分类) -> 默认为 `article` (稍后可手动调整为视频)
*   **内容创作 IP**:
    *   原 `category: 'cc-video'` -> 改为 `category: 'content-creation'`, `resourceType: 'video'`
    *   原 `category: 'cc-podcast'` -> 改为 `category: 'content-creation'`, `resourceType: 'podcast'`
    *   原 `category: 'cc-article'` -> 改为 `category: 'content-creation'`, `resourceType: 'article'`

#### 2. 标签管理增强 (Tag Management)
用户希望“按标签管理”。我将在资源列表页增加**标签筛选功能**：
*   在列表顶部增加一个“标签筛选”下拉框。
*   自动提取当前列表中的所有标签供选择。
*   选中标签后，列表只显示带有该标签的资源。

#### 3. 验证
*   执行迁移后，您将能立即在后台看到所有数据。
*   您可以在列表页测试标签筛选功能。