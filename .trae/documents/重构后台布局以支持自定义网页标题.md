您希望修改**后台网页的 Title**（浏览器标签页显示的标题），将其改为“**web网站管理系统**”。

目前 `src/app/admin/layout.tsx` 是一个客户端组件（因为包含了交互式的侧边栏），无法直接导出 SEO Metadata。

### 修复计划
为了设置独立的后台标题，我需要进行一次架构重构：

1.  **抽离侧边栏**：将现有的侧边栏逻辑（包含导航、折叠菜单、退出登录等）移动到一个新的客户端组件 `src/components/admin/AdminSidebar.tsx` 中。
2.  **重构后台布局**：将 `src/app/admin/layout.tsx` 改回**服务端组件**。
3.  **设置 Metadata**：在新的服务端布局中，导出 `metadata` 对象，设置 `title: 'web网站管理系统'`。

这样既保留了侧边栏的交互功能，又能完美控制后台页面的 SEO 标题。