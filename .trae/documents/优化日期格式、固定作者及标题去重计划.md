您反馈了三个需求：
1.  **日期格式优化**：将原本冗长的 ISO 时间（如 `2026-01-16T15:00...`）改为易读的年月日格式（如 `2026年01月16日`）。
2.  **固定作者**：将所有博客文章的作者固定显示为“阿波”，而非“管理员”或从数据库读取。
3.  **自动去重标题**：当系统自动从正文提取标题后，**自动删除正文中的该标题行**，避免正文开头重复出现一次标题。

### 实施计划

1.  **修改前端展示组件**：
    *   **ResourceCard.tsx** (列表页)：使用 `date-fns` 或原生 `Intl` 格式化日期。
    *   **BlogPage [slug]** (详情页)：同样格式化日期，并将作者显示硬编码为“阿波”。

2.  **修改后台编辑逻辑 (PostForm.tsx)**：
    *   在提取标题的逻辑中增加一步：一旦提取成功，将正文的第一行内容移除。
    *   这需要小心处理，确保只有当第一行确实是标题（例如以 `#` 开头）时才删除，避免误删正文。

3.  **验证**：确认前台显示的日期格式正确，且新建文章时正文不再包含标题。