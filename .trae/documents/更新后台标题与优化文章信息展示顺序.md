您希望对后台和前台进行以下修改：

1. **后台系统网站标题**：将“阿波的超级个体空间”更改为“**web网站管理系统**”。
2. **博客文章 Meta 信息优化**：

   * **删除图标**：去掉“阿波”前面的圆形“阿”字图标。

   * **调整顺序**：改为 **分类 -> 时间 -> 作者**。

### 实施计划

1. **修改后台布局** (`src/app/admin/layout.tsx`)：

   * 找到侧边栏顶部的标题区域，更新文本。

2. **修改博客详情页** (`src/app/blog/[slug]/page.tsx`)：

   * 重构 Meta 信息区域的 JSX 结构。

   * 先判断并渲染 `category`（如果存在）。

   * 接着渲染 `date`。

   * 最后渲染 `author`（仅文字“阿波”）。

   * 调整中间的分隔符 `|` 逻辑，确保显示正确。

3. **验证**：确认修改后的视觉效果符合您的要求。

