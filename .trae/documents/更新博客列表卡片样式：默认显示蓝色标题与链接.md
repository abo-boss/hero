您希望将博客列表中的“文章标题”和“阅读更多”的颜色，**默认状态**就改为之前的悬停颜色（即蓝色）。

### 实施计划

1.  **修改 `src/components/ResourceCard.tsx`**：
    *   **标题**：将 `text-slate-900 group-hover:text-blue-600` 更改为直接使用 `text-blue-600`。
    *   **阅读更多**：将 `text-slate-400 group-hover:text-blue-600` 更改为直接使用 `text-blue-600`。
    *   移除不必要的 `group-hover` 类，因为默认已经是蓝色了。

### 预期效果
打开博客列表页时，所有文章的标题和底部的“阅读更多”链接都会直接显示为醒目的蓝色，无需鼠标悬停。