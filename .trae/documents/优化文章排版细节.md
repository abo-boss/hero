您希望进一步微调文章详情页的排版：

1. **标题**：调小字号。
2. **信息布局**：时间和作者合并到一行，显示在标题下方。
3. **分割线**：改为细长线。
4. **正文**：统一行间距。

### 实施计划

1. **调整** **`src/app/blog/[slug]/page.tsx`**：

   * **Header 部分**：

     * 移除顶部的 Category/Date 栏。

     * 缩小 `h1` 标题字号 (从 `text-5xl` 降到 `text-3xl/4xl`)。

     * 在标题下方创建新的元信息行，包含：作者头像/名字 + 分隔点 + 发布时间。

   * **分割线**：

     * 将 `w-12 h-1` (短粗) 改为 `w-full h-px` (全宽细线)。

   * **正文排版**：

     * 确认 `prose-p:leading-8` (行高) 已经在用，可以微调为 `leading-relaxed` 以确保一致性。

2. **验证**：确认视觉效果符合“极简”要求。

