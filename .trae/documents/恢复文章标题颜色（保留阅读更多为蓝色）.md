您希望将博客文章的标题颜色恢复为之前的深灰色/黑色样式，但**保留**“阅读更多”为蓝色。

这意味着：
1.  **博客列表页** (`src/components/ResourceCard.tsx`)：
    *   **文章标题**：恢复为默认深色 (`text-slate-900`)，悬停时变蓝 (`group-hover:text-blue-600`)。
    *   **阅读更多**：保持目前的蓝色 (`text-blue-600`)。

2.  **博客详情页** (`src/app/blog/[slug]/page.tsx`)：
    *   **文章大标题**：恢复为深色 (`text-slate-900`)。

### 实施计划

1.  **修改 `src/components/ResourceCard.tsx`**：
    *   将标题的 `text-blue-600` 改回 `text-slate-900 group-hover:text-blue-600`。

2.  **修改 `src/app/blog/[slug]/page.tsx`**：
    *   将标题的 `text-blue-600` 改回 `text-slate-900`。

### 预期效果
*   列表页：标题默认黑色（悬停变蓝），“阅读更多”常驻蓝色。
*   详情页：大标题恢复黑色。