您希望清空目前资源库的所有内容。

我们将执行以下步骤：
1.  **创建删除脚本**：编写 `scripts/delete-resources.ts`，专门用于从数据库中删除所有 `type` 为 `resource` 的条目（包括 AI 视频、工具、播客等）。
2.  **执行删除**：运行该脚本，彻底清空数据库中的资源数据。
3.  **清理缓存**：为了防止前台页面仍显示旧数据的缓存，我们将清理 `.next` 缓存并重启服务。

这样做将彻底重置您的资源库，使其变为空白状态，方便您重新录入数据。