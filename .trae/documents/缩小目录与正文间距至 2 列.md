您觉得 4 列的间距太大了，希望**缩小 2 列**，即让间距变为 **2 列**。

### 布局方案调整 (Grid 12 列)

新的分配方案如下：

1.  **左侧目录**：保持 **2 列** (`col-span-2`)。
2.  **中间留白**：缩减为 **2 列**（通过让正文从第 5 列开始实现）。
3.  **正文内容**：宽度增加到 **8 列** (`col-span-8`)。
    *   (2 + 2 + 8 = 12 列，占满全宽)

### 实施步骤
1.  **修改 `src/app/blog/[slug]/page.tsx`**：
    *   将 `<article>` 的起始位置改为 `lg:col-start-5`（原本是 7）。
    *   将 `<article>` 的宽度改为 `lg:col-span-8`（原本是 6）。

### 预期效果
*   间距适中（2 列），既有区分度又不会太疏离。
*   正文变宽（8 列），阅读体验更好，每行能容纳更多文字。